import { AdminApp } from '@/components/admin/AdminApp'

export const metadata = {
  title: 'Admin — ReentryIQ',
  description: 'Platform administration: users, attestations, and data.',
}

export default function AdminPage() {
  return <AdminApp />
}
